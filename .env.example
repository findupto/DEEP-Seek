# Optional enterprise API settings
POS_API_TOKEN=replace-with-a-long-random-secret
POS_CORS=http://localhost:8080
# Set only for a deliberately isolated development environment.
POS_API_ALLOW_ANONYMOUS=false

# Optional database / server settings
# POS_DB=pos.db
# DATABASE_URL=postgresql://user:password@host:5432/database
# POS_HOST=0.0.0.0
# POS_PORT=8080

# Optional encrypted backup password
# POS_BACKUP_PASSWORD=replace-with-a-strong-secret
